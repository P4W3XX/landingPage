{
  "buildCommand": "npm run build",
  "installCommand": "npm install",
  "outputDirectory": "dist/client",
  "framework": "other",
  "rewrites": [
    {
      "source": "/:path*",
      "destination": "/api"
    }
  ]
}
